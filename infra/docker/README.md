# Docker

Local Docker templates for development.

## Confidentiality

Do not commit database passwords, connection strings, host paths, production
ports, private model endpoints, or environment-specific values in this README.

Use `.env.example` for non-sensitive placeholders and keep real values in
ignored local `.env` files.
